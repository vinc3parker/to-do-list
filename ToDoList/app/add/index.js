import react from "react";
import { View, Text, SafeAreaView } from "react-native";

const Add = () => {
    return (
      <SafeAreaView>
        <Text>Add Page</Text>
      </SafeAreaView>
    );
};

export default Add;