import { FlatList, TextInput, View, StyleSheet } from "react-native-gesture-handler";
import react from "react";

const Slider = () => {
    return (
        <View>
            <FlatList data={SliderInfo} renderItem={({ item, index } ) => />
        </View>
    )
}